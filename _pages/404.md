---
title: "Page not found"
layout: single
excerpt: ""
sitemap: false
permalink: /404.html
classes: not-found-page
---

<style>
  body.not-found-page .page__title { display: none !important; }
  body.not-found-page .page__hero { display: none !important; }
  body.not-found-page .page__hero--overlay { display: none !important; }
  body.not-found-page .initial-content { padding-top: 0 !important; margin-top: 0 !important; }
  body.not-found-page .page { width: 100% !important; padding-right: 0 !important; }
  body.not-found-page .page__inner-wrap { width: 100% !important; max-width: 100% !important; }
  body.not-found-page .page__content { width: 100% !important; max-width: 100% !important; padding: 0 !important; }
  body.not-found-page .archive { width: 100% !important; max-width: 100% !important; padding: 0 !important; margin: 0 !important; }
  body.not-found-page #main { max-width: 100% !important; padding: 0 !important; }

  .nf-wrap {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 6rem 1.5rem;
    background:
      radial-gradient(ellipse 70% 50% at 50% 50%, rgba(4, 169, 255, 0.10), transparent 70%),
      var(--bg-primary);
    overflow: hidden;
  }
  .nf-wrap::before {
    content: "";
    position: absolute;
    top: 50%; left: 50%;
    transform: translate(-50%, -50%);
    width: 800px; height: 800px;
    background: radial-gradient(circle, rgba(4, 169, 255, 0.06) 0%, transparent 50%);
    pointer-events: none;
  }
  .nf-inner {
    position: relative;
    z-index: 1;
    text-align: center;
    max-width: 640px;
  }
  .nf-code {
    font-size: clamp(7rem, 18vw, 12rem);
    font-weight: 600;
    color: #04a9ff;
    letter-spacing: -0.05em;
    line-height: 0.9;
    margin: 0 0 1rem;
    background: linear-gradient(135deg, #33bbff 0%, #04a9ff 50%, #0090dd 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
  .nf-eyebrow {
    display: inline-block;
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--text-muted);
    margin-bottom: 1rem;
  }
  .nf-title {
    font-size: clamp(1.5rem, 3.5vw, 2.4rem);
    font-weight: 600;
    letter-spacing: -0.035em;
    line-height: 1.1;
    margin: 0 0 1rem;
    color: var(--text-primary);
  }
  .nf-sub {
    font-size: 1.05rem;
    color: var(--text-secondary);
    line-height: 1.6;
    margin: 0 0 2.5rem;
  }
  .nf-ctas {
    display: flex;
    gap: 0.75rem;
    justify-content: center;
    flex-wrap: wrap;
  }
  .nf-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    padding: 0.95rem 1.6rem;
    border-radius: var(--radius-sm);
    font-family: var(--font-sans);
    font-size: 0.95rem;
    font-weight: 600;
    letter-spacing: -0.01em;
    text-decoration: none !important;
    border: 1px solid transparent;
    cursor: pointer;
    transition: all 0.2s var(--ease-out-expo);
    min-height: 48px;
    white-space: nowrap;
  }
  .nf-btn--primary {
    background: #04a9ff;
    color: #0a0a0f !important;
  }
  .nf-btn--primary:hover {
    background: #33bbff;
    transform: translateY(-1px);
    box-shadow: 0 8px 24px rgba(4, 169, 255, 0.25);
  }
  .nf-btn--ghost {
    background: rgba(255, 255, 255, 0.04);
    color: var(--text-primary) !important;
    border-color: rgba(255, 255, 255, 0.12);
  }
  .nf-btn--ghost:hover {
    background: rgba(255, 255, 255, 0.08);
    border-color: rgba(255, 255, 255, 0.22);
    transform: translateY(-1px);
  }
  .nf-btn i { transition: transform 0.2s var(--ease-out-expo); }
  .nf-btn:hover i { transform: translateX(3px); }

  .nf-links {
    margin-top: 3rem;
    padding-top: 2rem;
    border-top: 1px solid var(--border-subtle);
  }
  .nf-links__label {
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.18em;
    color: var(--text-muted);
    text-transform: uppercase;
    margin-bottom: 0.85rem;
  }
  .nf-links__row {
    display: flex;
    gap: 0.5rem;
    justify-content: center;
    flex-wrap: wrap;
  }
  .nf-link {
    display: inline-flex;
    align-items: center;
    padding: 0.5rem 1rem;
    background: var(--bg-glass);
    border: 1px solid var(--border-subtle);
    border-radius: 100px;
    font-size: 0.8rem;
    font-weight: 500;
    color: var(--text-secondary);
    text-decoration: none !important;
    transition: all 0.2s var(--ease-out-expo);
  }
  .nf-link:hover {
    background: rgba(4, 169, 255, 0.1);
    border-color: rgba(4, 169, 255, 0.3);
    color: #04a9ff;
  }

  .nf-eq {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    align-items: flex-end;
    gap: 4px;
    height: 28px;
    opacity: 0.14;
    pointer-events: none;
  }
  .nf-eq span {
    display: block;
    width: 3px;
    background: linear-gradient(180deg, transparent, #04a9ff 30%, #04a9ff 70%, transparent);
    border-radius: 2px;
    animation: nfEqBar 1.4s ease-in-out infinite;
  }
  .nf-eq span:nth-child(1)  { height: 12px; animation-delay: 0.0s; }
  .nf-eq span:nth-child(2)  { height: 20px; animation-delay: 0.15s; }
  .nf-eq span:nth-child(3)  { height: 8px;  animation-delay: 0.3s; }
  .nf-eq span:nth-child(4)  { height: 24px; animation-delay: 0.1s; }
  .nf-eq span:nth-child(5)  { height: 14px; animation-delay: 0.45s; }
  .nf-eq span:nth-child(6)  { height: 18px; animation-delay: 0.25s; }
  .nf-eq span:nth-child(7)  { height: 26px; animation-delay: 0.05s; }
  .nf-eq span:nth-child(8)  { height: 10px; animation-delay: 0.4s; }
  .nf-eq span:nth-child(9)  { height: 22px; animation-delay: 0.2s; }
  .nf-eq span:nth-child(10) { height: 16px; animation-delay: 0.35s; }
  @keyframes nfEqBar {
    0%, 100% { transform: scaleY(0.4); }
    50%      { transform: scaleY(1.6); }
  }
</style>

<div class="nf-wrap">
  <div class="nf-inner">
    <div class="nf-eyebrow">404 · Page not found</div>
    <div class="nf-code">404</div>
    <h1 class="nf-title">The track ended early.</h1>
    <p class="nf-sub">This page doesn't exist or was moved. Here's where to go.</p>
    <div class="nf-ctas">
      <a href="/" class="nf-btn nf-btn--primary">
        Back to home <i class="fas fa-arrow-right"></i>
      </a>
      <a href="/contact/" class="nf-btn nf-btn--ghost">
        Get in touch
      </a>
    </div>
    <div class="nf-links">
      <div class="nf-links__label">Or jump to</div>
      <div class="nf-links__row">
        <a href="/about/" class="nf-link">About</a>
        <a href="/services/" class="nf-link">Services</a>
        <a href="/photos/" class="nf-link">Photos</a>
      </div>
    </div>
  </div>
  <div class="nf-eq" aria-hidden="true">
    <span></span><span></span><span></span><span></span><span></span>
    <span></span><span></span><span></span><span></span><span></span>
  </div>
</div>
